name: ci
on:
  pull_request:
    types: [opened, synchronize]

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Check scripts with shellcheck
      run: shellcheck zsh-select-commit-prefix/zsh-select-commit-prefix.plugin.zsh